*** Update File: docubot/backend/database.py
@@
- created_at = Column(DateTime, default=datetime.utcnow, index=True) # ✅ Индекс для сортировки
+ created_at = Column(DateTime, default=datetime.utcnow, index=True) # ✅ Индекс для сортировки
+ reset_token = Column(String, nullable=True, index=True)
+ reset_token_expiry = Column(DateTime, nullable=True)
---
*** Update File: docubot/backend/auth.py
@@
-import os
-import jwt
-import secrets
-from datetime import datetime, timedelta
-import loggingЫ
-from datetime import datetime, timedelta
+import os
+import jwt
+import secrets
+from datetime import datetime, timedelta
+import logging
@@
-# 🔧 Импортируем User и get_db из database.py (не определяем User заново!)
+# 🔧 Импортируем User и get_db из database.py (не определяем User заново!)
+logger = logging.getLogger(__name__)
@@
-class UserResponse(BaseModel):
+class UserResponse(BaseModel):
id: int
email: str
full_name: Optional[str]
is_active: bool
created_at: datetime

- class Config:
- from_attributes = True
+ class Config:
+ orm_mode = True
@@
-class PasswordResetRequest(BaseModel):
- email: EmailStr
+class PasswordResetRequest(BaseModel):
+ email: EmailStr
@@
-class PasswordResetConfirm(BaseModel):
- token: str
- new_password: str
+class PasswordResetConfirm(BaseModel):
+ token: str
+ new_password: str